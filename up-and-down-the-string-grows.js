/* Many programming languages provide the functionality of converting a string to uppercase or lowercase. Typically, these methods won't change the size of the string. However, in some special cases, the length of the transformed string may be longer than the original.

Since the following string contains special characters. When STRANGE_STRING.toUpperCase().toLowerCase().length; is executed. More than 1 character is returned.

*/

const STRANGE_STRING = "İ"
